// import Cell from './cell'
import { VNode } from 'vue'
import { getOptionProps, getSlot, getClass, getStyle, getComponent } from './_util/props-util'

export default {
  name: 'q-description-row',
  functional: true,
  // render: (h, ctx) => {
    // const renderCells = (
    //   items: VNode[],
    //   { colon, prefixCls, bordered },
    //   { component, type, showLabel, showContent }
    // ) => {
    //   return items.map((item, index) => {
    //     const { prefixCls, span = 1 } = getOptionProps(item)
    //     const label = getComponent(item, 'label')

    //   })
    // }
  // }
}